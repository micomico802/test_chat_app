name: test_chat_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  intl: any
  firebase_core: ^2.6.1
  firebase_core_web: ^2.2.0
  cloud_firestore: ^4.3.2
  firebase_in_app_messaging: ^0.7.0+12
  firebase_app_installations: ^0.2.1+10
  shared_preferences: ^2.0.17
  firebase_app_check: ^0.1.1+12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
